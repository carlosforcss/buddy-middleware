# Frontend Configuration
FRONTEND_HOST=host.docker.internal
FRONTEND_PORT=3000

# Backend Configuration  
BACKEND_HOST=host.docker.internal
BACKEND_PORT=8000

# Alternative: If your services are in other Docker containers on the same network
# FRONTEND_HOST=frontend_container_name
# BACKEND_HOST=backend_container_name

# Alternative: If your services are on different hosts
# FRONTEND_HOST=192.168.1.100
# BACKEND_HOST=192.168.1.101 